/* 
SERVICE.JS (Model)

Responsável por lidar com as requisições à PokeAPI.

O que deve ser feito aqui:
1️ Criar uma função assíncrona (ex: buscarPokemon(nome)) que:
    - Use 'fetch()' para buscar dados da PokeAPI: https://pokeapi.co/api/v2/pokemon/{nome}
    - Retorne os dados JSON do Pokémon.
    - Trate erros de resposta inválida (exibir "Pokémon não encontrado").
2️ Exportar essa função para que o controller.js possa utilizá-la.
*/
